export type Outlet = {
  id: string;
  name: string;
  location: string;
  hours: string;
};

export type SoupPack = {
  id: string;
  title: string;
  subtitle: string;
  price: number;
  image: string;
};

export type FoodItem = {
  id: string;
  name: string;
  price: number;
  category: string;
};
